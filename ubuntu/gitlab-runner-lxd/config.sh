#!/bin/bash
source /opt/gitlab-runner-lxd/base.sh

# currently, this does not need to do anything. this is here to satisfy the
# whole contract between gitlab-runner and this custom runner.

echo '{}'
